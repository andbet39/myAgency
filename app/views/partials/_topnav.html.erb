<nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar bar1"></span>
                    <span class="icon-bar bar2"></span>
                    <span class="icon-bar bar3"></span>
                </button>
                <a class="navbar-brand" ><%= content_for?(:title) ? yield(:title) :'' %></a>
                <% if @has_help %>
                  <a class="btn btn-large btn-success" href="javascript:void(0);" onclick="javascript:introJs().start();">Aiuto</a>
                <% end %>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                  <% if current_user.notification_to_read.count >0 %>

                  <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                              <%if current_user.notification_to_read.count > 0 %>
                                <i style="color:red" class="fa fa-bell"></i>
                                <% else %>
                                <i  class="fa fa-bell"></i>
                              <%end %>
                              <p class="notification"><%= current_user.notification_to_read.count %></p>
            <p>Notifiche</p>
            <b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu">
                          <% current_user.notification_to_read.each() do |n| %>
                            <li>
                              <a href="/notification/view?notification_id=<%=n.id %>"><%= n.body %> <small class="timestring"><%= n.created_at %></small></a>
                            </li>


                          <% end %>
                        </ul>
                  </li>
                  <% end %>
                    <li class="dropdown">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <p><%= current_user.displayname %></p>
                              <b class="caret"></b>
                          </a>
                          <ul class="dropdown-menu">
                            <li><a href="/profiles/<%= current_user.profile.id %>/edit">  Profilo</a></li>
                             <li><a href="/users/sign_out "><i class="fa fa-sign-out pull-right"></i> Log Out</a></li>

                          </ul>
                    </li>
                </ul>

            </div>
        </div>
    </nav>
